{
  "name": "@landmatch/extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build && BUILD_TARGET=content vite build",
    "lint": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@landmatch/api": "workspace:*",
    "@landmatch/scoring": "workspace:*",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.4",
    "@types/chrome": "^0.0.304",
    "jsdom": "^29.0.2",
    "typescript": "~5.9.3",
    "vite": "^6.3.5",
    "vitest": "^4.0.16"
  }
}
